import 'package:flutter/material.dart';

class BodyConsultoria extends StatelessWidget {
  const BodyConsultoria({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
